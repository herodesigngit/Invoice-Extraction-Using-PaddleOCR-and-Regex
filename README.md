# Invoice-Extraction-Using-PaddleOCR-and-Regex
A Python-based solution that automatically extracts key invoice data such as vendor name, invoice number, date, and total amount from scanned or image-based invoices using PaddleOCR for text recognition and Regex for structured information extraction.
